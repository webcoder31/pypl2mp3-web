# Coquille web (FastAPI, jobs, WebSocket) — plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Servir une interface web locale par `pypl2mp3 ui`, avec un registre de tâches longues et une progression poussée en direct — le tout exercé par un consommateur réel, pas seulement construit.

**Architecture:** FastAPI sur uvicorn, rendu Jinja2 + HTMX sans étape de build. Les tâches longues vivent dans un registre `asyncio` en mémoire et s'exécutent via `asyncio.to_thread`, parce que `libs/song.py` bloque de façon synchrone. `WebProgress` implémente le `ProgressPort` du plan 1 et remet ses événements à la boucle de façon sûre entre threads.

**Tech Stack:** FastAPI 0.125, uvicorn, Jinja2, HTMX (versionné), httpx (tests), pytest.

**Plan précédent:** `docs/superpowers/plans/2026-08-12-foundations.md` — terminé
**Spec de référence:** `docs/superpowers/specs/2026-08-11-web-ui-design.md`

## Global Constraints

- **Répertoire de travail : `/Users/thierry/Code/projects/pypl2mp3-web`.** Le dossier voisin `/Users/thierry/Code/projects/pypl2mp3` est le **projet gelé** : ne jamais y écrire. Vérifier `pwd` avant toute modification.
- Python requis : `~=3.13.0`. Ne pas élargir.
- **`src/pypl2mp3/libs/` reste inchangé.** Aucune exception.
- **`pydantic` est bloqué en 1.x par `shazamio`.** N'employer aucune API pydantic v2 : pas de `model_validate`, `model_dump`, `ConfigDict`, ni de validateurs de style `Annotated`. Écrire les modèles en syntaxe 1.x (`class Config`, `parse_obj`, `dict()`).
- **Le serveur écoute sur `127.0.0.1` exclusivement.** Jamais `0.0.0.0`, jamais une valeur configurable qui permettrait l'exposition réseau par inadvertance.
- **Aucun CDN.** HTMX est versionné dans le dépôt. Aucune requête sortante à l'exécution de l'interface.
- **Aucune base de données.** Le système de fichiers est la source de vérité ; l'état des jobs est éphémère, en mémoire.
- `ProgressPort` : méthodes **synchrones et non bloquantes**. `InteractionPort.ask` : **`async`**.
- **Ne jamais passer `client="WEB"`** à `YouTube()` ou `Playlist()`.
- **Tout accès à un attribut pytubefix doit se trouver dans un bloc `try` protégé.** Le constructeur ne fait aucune E/S.
- **Commentaires, docstrings et messages de commit en anglais.** La tâche 1 y convertit l'existant ; tout code écrit ensuite naît en anglais.
- Dépendances gérées par `uv` : `uv run`, `uv sync`, `uv add`. Jamais `pip` ni `python` nus.

---

### Task 1: Convertir les modules du plan 1 en anglais

Le dépôt est public et son README, ses commits, `libs/` et `commands/` sont en anglais. Les quatre modules livrés au plan 1 sont en français. Convertir maintenant coûte peu ; après huit services et la couche web, beaucoup.

**Files:**
- Modify: `src/pypl2mp3/ports/interaction.py`
- Modify: `src/pypl2mp3/ports/progress.py`
- Modify: `src/pypl2mp3/services/_song_callbacks.py`
- Modify: `src/pypl2mp3/services/list_playlists.py`
- Modify: `src/pypl2mp3/commands/list_playlists.py`
- Modify: `tests/doubles.py`, `tests/test_interaction_port.py`, `tests/test_progress_port.py`, `tests/test_song_callbacks.py`, `tests/test_list_playlists_service.py`, `tests/test_import_playlist_resilience.py`

**Interfaces:**
- Consumes: rien.
- Produces: aucun changement d'API. **Aucun nom d'identifiant ne change** — uniquement les commentaires et docstrings.

- [ ] **Step 1: Traduire les commentaires et docstrings**

Traduire en anglais tous les commentaires et docstrings de ces fichiers, en conservant le sens et la précision technique. Les explications qui portent une leçon durement acquise doivent survivre intactes — notamment, dans `_song_callbacks.py` :

- pourquoi les hooks Shazam sont `async` (song.py les attend avec `await`) ;
- pourquoi `use_default_verbosity=False` est obligatoire ;
- pourquoi `libs/song.py` n'est pas modifié.

et dans `test_list_playlists_service.py`, pourquoi le piège réseau couvre trois vecteurs.

**Ne changer aucun identifiant.** Les noms de fonctions, variables, classes et fichiers restent tels quels.

**Une exception à traiter avec soin :** `tests/test_list_playlists_service.py` contient
`raise AssertionError("un listing local a tenté un accès réseau")`, et deux tests
font `pytest.raises(AssertionError, match="accès réseau")`. Le message ET les deux
`match=` doivent changer ensemble, sinon les tests cassent. Suggestion :
`"a local listing attempted network access"` et `match="network access"`.

- [ ] **Step 2: Vérifier qu'il ne reste rien en français**

Run: `grep -rnE "[éèêàçùôîû]" src/pypl2mp3/ports/ src/pypl2mp3/services/ tests/ src/pypl2mp3/commands/list_playlists.py`
Expected: aucune sortie.

Ce filtre attrape les accents, pas les mots français sans accent. Relire aussi les fichiers.

- [ ] **Step 3: Lancer la suite**

Run: `uv run pytest -v`
Expected: 32 passed. Une traduction ne change aucun comportement ; tout écart signale une erreur.

- [ ] **Step 4: Commit**

```bash
git add src/ tests/
git commit -m "docs: switch the new modules to English"
```

---

### Task 2: Dépendances web et commande `ui`

**Files:**
- Modify: `pyproject.toml`
- Create: `src/pypl2mp3/web/__init__.py`
- Create: `src/pypl2mp3/web/app.py`
- Modify: `src/pypl2mp3/main.py`
- Create: `tests/test_web_app.py`

**Interfaces:**
- Consumes: rien du plan 1.
- Produces:
  - `pypl2mp3.web.app.create_app() -> fastapi.FastAPI` — fabrique l'application ; prend le chemin du dépôt.
  - `pypl2mp3.web.app.serve(repository_path: Path, port: int) -> None` — démarre uvicorn sur `127.0.0.1`.
  - sous-commande CLI `ui`, avec `-p/--port` (défaut `8731`).

- [ ] **Step 1: Ajouter les dépendances**

Run: `uv add fastapi uvicorn jinja2` puis `uv add --dev httpx`
Expected: résolution sans conflit. Versions attendues : fastapi 0.125.x, starlette 0.50.x, uvicorn 0.52.x, jinja2 3.1.x, httpx 0.28.x. `anyio` doit rester en 3.7.1 et `shazamio` en 0.4.0.1 — si `uv` propose de les changer, s'arrêter et signaler.

- [ ] **Step 2: Écrire le test**

Créer `tests/test_web_app.py` :

```python
"""The web application must serve locally and never bind a public interface."""

import inspect
from pathlib import Path

import httpx

from pypl2mp3.web.app import create_app, serve


def test_create_app_returns_a_fastapi_application(tmp_path):
    app = create_app(tmp_path)

    assert app.title == "PYPL2MP3"


async def test_health_endpoint_reports_the_repository(tmp_path):
    # An ASYNC client is required: httpx.ASGITransport only implements the
    # async transport interface, so a sync httpx.Client cannot drive it.
    # `asyncio_mode = "auto"` means no decorator is needed here.
    app = create_app(tmp_path)
    transport = httpx.ASGITransport(app=app)

    async with httpx.AsyncClient(
        transport=transport, base_url="http://test"
    ) as client:
        response = await client.get("/health")

    assert response.status_code == 200
    assert response.json() == {"status": "ok", "repository": str(tmp_path)}


def test_serve_binds_the_loopback_interface_only():
    """A local tool must never be reachable from the network by accident."""

    source = inspect.getsource(serve)

    assert "127.0.0.1" in source
    assert "0.0.0.0" not in source
```

- [ ] **Step 3: Lancer le test pour le voir échouer**

Run: `uv run pytest tests/test_web_app.py -v`
Expected: FAIL avec `ModuleNotFoundError: No module named 'pypl2mp3.web'`

- [ ] **Step 4: Écrire l'application**

Créer `src/pypl2mp3/web/__init__.py`, vide, puis `src/pypl2mp3/web/app.py` :

```python
#!/usr/bin/env python3
"""FastAPI application serving the local web interface.

The server binds the loopback interface only. This is a single-user local
tool: it must never become reachable from the network because someone
passed the wrong flag.
"""

from pathlib import Path

import uvicorn
from fastapi import FastAPI

# Arbitrary, memorable, unlikely to collide with a dev server.
DEFAULT_PORT = 8731


def create_app(repository_path: Path) -> FastAPI:
    """Build the application for a given playlist repository.

    Args:
        repository_path: folder where playlists are stored.

    Returns:
        A configured FastAPI application.
    """

    app = FastAPI(title="PYPL2MP3", docs_url=None, redoc_url=None)
    app.state.repository_path = Path(repository_path)

    @app.get("/health")
    def health() -> dict[str, str]:
        return {
            "status": "ok",
            "repository": str(app.state.repository_path),
        }

    return app


def serve(repository_path: Path, port: int = DEFAULT_PORT) -> None:
    """Run the server until interrupted.

    Binds 127.0.0.1 unconditionally — the host is deliberately not a
    parameter.
    """

    uvicorn.run(
        create_app(repository_path),
        host="127.0.0.1",
        port=port,
        log_level="warning",
    )
```

- [ ] **Step 5: Câbler la sous-commande `ui`**

Dans `src/pypl2mp3/main.py`, ajouter un lanceur à côté des autres `_run_*` (après `_run_play_songs`, vers la ligne 158) :

```python
def _run_web_ui(args: argparse.Namespace) -> None:
    """
    Runner for the "ui" command.

    Args:
        args: Parsed arguments.
    """

    from pathlib import Path

    from pypl2mp3.web.app import serve
    serve(Path(args.repo), args.port)
```

Puis, après le bloc du sous-parseur `play` (qui se termine par `play_songs_command.set_defaults(...)`, vers la ligne 603), ajouter :

```python
    # CLI parser for command "ui"
    web_ui_command = subparsers.add_parser(
        'ui',
        parents=[shared_options_parser],
        help='Serve the web interface locally',
        description='Serve the web interface locally',
        epilog=epilog_md,
        formatter_class=cliParser.formatter_class
    )
    web_ui_command.add_argument(
        "-p", "--port",
        metavar="port",
        type=int,
        default=8731,
        help="Port to listen on (default: 8731)"
    )
    web_ui_command.set_defaults(func=_run_web_ui)
```

Le préambule de `main()` gère déjà les commandes sans argument `playlist` : `selected_playlist` vaut `"All"` par défaut (`main.py:648`). Aucune autre modification n'est nécessaire.

- [ ] **Step 6: Lancer les tests**

Run: `uv run pytest tests/test_web_app.py -v`
Expected: 3 PASS

- [ ] **Step 7: Vérifier le serveur pour de vrai**

```bash
uv run pypl2mp3 ui &
sleep 3
curl -s http://127.0.0.1:8731/health
curl -s --max-time 2 http://$(ipconfig getifaddr en0 2>/dev/null || echo 127.0.0.2):8731/health && echo "JOIGNABLE DE L'EXTERIEUR — PROBLEME" || echo "non joignable hors loopback, correct"
kill %1
```
Expected: le premier `curl` renvoie le JSON de santé ; le second échoue.

- [ ] **Step 8: Commit**

```bash
git add pyproject.toml uv.lock src/pypl2mp3/web/ src/pypl2mp3/main.py tests/test_web_app.py
git commit -m "feat: serve a local web application with the ui command"
```

---

### Task 3: Page d'inventaire des playlists

Premier consommateur réel : la vue rend le service `list_playlists` du plan 1, sans aucun appel réseau.

**Files:**
- Create: `src/pypl2mp3/web/templates/base.html`
- Create: `src/pypl2mp3/web/templates/playlists.html`
- Create: `src/pypl2mp3/web/static/htmx.min.js`
- Modify: `src/pypl2mp3/web/app.py`
- Create: `tests/test_web_playlists.py`

**Interfaces:**
- Consumes: `pypl2mp3.services.list_playlists.list_playlists(Path) -> list[PlaylistSummary]`, dont les champs sont `path`, `playlist_id`, `name`, `total_songs`, `junk_songs`, et la propriété `valid_songs`.
- Produces: route `GET /` rendant l'inventaire.

- [ ] **Step 1: Verser HTMX dans le dépôt**

Télécharger HTMX 2.x en fichier versionné, pas en CDN :

```bash
mkdir -p src/pypl2mp3/web/static
curl -sL https://unpkg.com/htmx.org@2.0.4/dist/htmx.min.js \
  -o src/pypl2mp3/web/static/htmx.min.js
wc -c src/pypl2mp3/web/static/htmx.min.js
```
Expected: environ 50 000 octets. Si le fichier fait moins de 10 000 octets, le téléchargement a échoué — vérifier avant de continuer.

- [ ] **Step 2: Écrire le test**

Créer `tests/test_web_playlists.py` :

```python
"""The inventory page renders local data and touches no network."""

import socket

import httpx

from pypl2mp3.web.app import create_app


def _forbidden(*args, **kwargs):
    raise AssertionError("the web inventory attempted network access")


def _block_network(monkeypatch):
    monkeypatch.setattr(socket.socket, "connect", _forbidden)
    monkeypatch.setattr(socket.socket, "connect_ex", _forbidden)
    monkeypatch.setattr(socket, "getaddrinfo", _forbidden)


def _make_playlist(repo, name, songs, junks):
    folder = repo / name
    folder.mkdir()
    for index in range(songs):
        (folder / f"ARTIST - Song {index} [vid{index:07d}].mp3").touch()
    for index in range(junks):
        (folder / f"ARTIST - Junk {index} [jnk{index:07d}] (JUNK).mp3").touch()


async def _get(app, path):
    """Fetch a page.

    httpx.ASGITransport implements only the async transport interface, so a
    sync httpx.Client cannot drive it. `asyncio_mode = "auto"` means the
    tests below need no decorator.
    """

    transport = httpx.ASGITransport(app=app)
    async with httpx.AsyncClient(
        transport=transport, base_url="http://test"
    ) as client:
        return await client.get(path)


async def test_inventory_lists_playlists_with_their_counts(
    tmp_path, monkeypatch
):
    _block_network(monkeypatch)
    _make_playlist(
        tmp_path, "Owner - Alpha [PL0000000000000000000000000000001]", 3, 1
    )

    response = await _get(create_app(tmp_path), "/")

    assert response.status_code == 200
    body = response.text
    assert "Owner - Alpha" in body
    assert "PL0000000000000000000000000000001" in body

    # Counts must appear as their own table cells, not merely somewhere in
    # the page: asserting "4" in body would pass on almost any markup.
    import re

    cells = re.findall(r'<td class="num[^"]*">\s*([0-9]+)\s*</td>', body)
    assert cells == ["3", "1", "4"], (
        f"expected tagged/junk/total = 3/1/4, got {cells}"
    )


async def test_inventory_reports_an_empty_repository(tmp_path, monkeypatch):
    _block_network(monkeypatch)

    response = await _get(create_app(tmp_path), "/")

    assert response.status_code == 200
    assert "No playlists" in response.text


async def test_htmx_is_served_locally(tmp_path):
    response = await _get(create_app(tmp_path), "/static/htmx.min.js")

    assert response.status_code == 200
    assert len(response.content) > 10_000


async def test_the_page_references_no_external_host(tmp_path, monkeypatch):
    """No CDN: the interface must work offline."""

    _block_network(monkeypatch)
    _make_playlist(
        tmp_path, "Owner - Alpha [PL0000000000000000000000000000001]", 1, 0
    )

    body = (await _get(create_app(tmp_path), "/")).text

    for marker in ("http://", "https://", "//unpkg", "//cdn"):
        assert marker not in body, f"external reference found: {marker}"
```

- [ ] **Step 3: Lancer le test pour le voir échouer**

Run: `uv run pytest tests/test_web_playlists.py -v`
Expected: FAIL — la route `/` n'existe pas encore (404).

- [ ] **Step 4: Écrire le gabarit de base**

Créer `src/pypl2mp3/web/templates/base.html` :

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}PYPL2MP3{% endblock %}</title>
    <script src="/static/htmx.min.js"></script>
    <style>
      :root { color-scheme: light dark; }
      body {
        font-family: system-ui, sans-serif;
        max-width: 60rem;
        margin: 2rem auto;
        padding: 0 1rem;
        line-height: 1.5;
      }
      h1 { font-size: 1.4rem; }
      table { border-collapse: collapse; width: 100%; }
      th, td { text-align: left; padding: 0.4rem 0.6rem; }
      tbody tr:nth-child(odd) { background: rgba(127, 127, 127, 0.08); }
      td.num { text-align: right; font-variant-numeric: tabular-nums; }
      .id { font-family: ui-monospace, monospace; font-size: 0.85em;
            opacity: 0.7; }
      .junk { color: #b26a00; }
    </style>
  </head>
  <body>
    <h1>{% block heading %}PYPL2MP3{% endblock %}</h1>
    {% block content %}{% endblock %}
  </body>
</html>
```

- [ ] **Step 5: Écrire le gabarit d'inventaire**

Créer `src/pypl2mp3/web/templates/playlists.html` :

```html
{% extends "base.html" %}

{% block content %}
  {% if not summaries %}
    <p>No playlists in this repository.</p>
  {% else %}
    <p>{{ summaries | length }} playlist(s) in {{ repository }}</p>
    <table>
      <thead>
        <tr>
          <th>Playlist</th>
          <th class="num">Tagged</th>
          <th class="num">Junk</th>
          <th class="num">Total</th>
        </tr>
      </thead>
      <tbody>
        {% for summary in summaries %}
          <tr>
            <td>
              {{ summary.name }}<br>
              <span class="id">{{ summary.playlist_id }}</span>
            </td>
            <td class="num">{{ summary.valid_songs }}</td>
            <td class="num {% if summary.junk_songs %}junk{% endif %}">
              {{ summary.junk_songs }}
            </td>
            <td class="num">{{ summary.total_songs }}</td>
          </tr>
        {% endfor %}
      </tbody>
    </table>
  {% endif %}
{% endblock %}
```

- [ ] **Step 6: Brancher gabarits, statiques et route**

Dans `src/pypl2mp3/web/app.py`, ajouter aux imports :

```python
from fastapi import FastAPI, Request
from fastapi.responses import HTMLResponse
from fastapi.staticfiles import StaticFiles
from fastapi.templating import Jinja2Templates

from pypl2mp3.services.list_playlists import list_playlists
```

et, dans `create_app`, avant le `return app` :

```python
    package_root = Path(__file__).parent
    app.mount(
        "/static",
        StaticFiles(directory=package_root / "static"),
        name="static",
    )
    templates = Jinja2Templates(directory=str(package_root / "templates"))

    @app.get("/", response_class=HTMLResponse)
    def inventory(request: Request) -> HTMLResponse:
        return templates.TemplateResponse(
            request,
            "playlists.html",
            {
                "summaries": list_playlists(app.state.repository_path),
                "repository": str(app.state.repository_path),
            },
        )
```

- [ ] **Step 7: Lancer les tests**

Run: `uv run pytest tests/test_web_playlists.py -v`
Expected: 4 PASS

- [ ] **Step 8: Vérifier dans un vrai navigateur**

```bash
uv run pypl2mp3 ui -r ~/Desktop/playlists
```
Ouvrir `http://127.0.0.1:8731/` et confirmer que les playlists s'affichent avec des compteurs cohérents avec `uv run pypl2mp3 playlists`. Consigner ce qui est observé.

- [ ] **Step 9: Commit**

```bash
git add src/pypl2mp3/web/ tests/test_web_playlists.py
git commit -m "feat: render the playlist inventory in the browser"
```

---

### Task 4: Registre de tâches longues

**Files:**
- Create: `src/pypl2mp3/web/jobs.py`
- Create: `tests/test_jobs.py`

**Interfaces:**
- Consumes: rien.
- Produces:
  - `JobState` — énumération : `PENDING`, `RUNNING`, `COMPLETED`, `FAILED`, `CANCELLED`
  - `Job` — porte `job_id: str`, `state: JobState`, `events: list[dict]` (tampon borné), `error: str | None`, `result: object | None`
  - `JobRegistry` avec `start(job_id, coro_factory) -> Job`, `get(job_id) -> Job | None`, `cancel(job_id) -> bool`, `emit(job_id, event: dict) -> None`
  - `JobAlreadyRunning(Exception)` — levée si `job_id` est déjà actif

- [ ] **Step 1: Écrire le test**

Créer `tests/test_jobs.py` :

```python
"""The job registry tracks long operations and survives browser reconnects."""

import asyncio

import pytest

from pypl2mp3.web.jobs import (
    Job,
    JobAlreadyRunning,
    JobRegistry,
    JobState,
)


async def test_a_completed_job_reports_its_result():
    registry = JobRegistry()

    async def work(job: Job) -> str:
        return "done"

    job = registry.start("j1", work)
    await registry.wait("j1")

    assert job.state is JobState.COMPLETED
    assert job.result == "done"
    assert job.error is None


async def test_a_failing_job_records_the_error_and_does_not_raise():
    registry = JobRegistry()

    async def work(job: Job) -> None:
        raise RuntimeError("boom")

    job = registry.start("j1", work)
    await registry.wait("j1")

    assert job.state is JobState.FAILED
    assert "boom" in job.error


async def test_events_are_buffered_so_a_reconnecting_client_catches_up():
    registry = JobRegistry()

    async def work(job: Job) -> None:
        registry.emit("j1", {"kind": "progress", "percent": 50.0})

    registry.start("j1", work)
    await registry.wait("j1")

    kinds = [event["kind"] for event in registry.get("j1").events]
    assert "progress" in kinds


def test_the_event_buffer_is_bounded():
    """An hours-long import must not grow the buffer without limit."""

    registry = JobRegistry(max_events=10)
    job = Job(job_id="j1", max_events=10)
    registry._jobs["j1"] = job

    for index in range(50):
        registry.emit("j1", {"kind": "progress", "n": index})

    assert len(job.events) == 10
    assert job.events[-1]["n"] == 49


async def test_starting_the_same_job_twice_is_refused():
    """Two concurrent imports of one playlist would download everything twice."""

    registry = JobRegistry()
    started = asyncio.Event()

    async def work(job: Job) -> None:
        started.set()
        await asyncio.sleep(1)

    registry.start("j1", work)
    await started.wait()

    with pytest.raises(JobAlreadyRunning):
        registry.start("j1", work)

    registry.cancel("j1")
    await registry.wait("j1")


async def test_a_cancelled_job_is_marked_cancelled_not_failed():
    registry = JobRegistry()
    started = asyncio.Event()

    async def work(job: Job) -> None:
        started.set()
        await asyncio.sleep(10)

    job = registry.start("j1", work)
    await started.wait()

    assert registry.cancel("j1") is True
    await registry.wait("j1")

    assert job.state is JobState.CANCELLED
```

- [ ] **Step 2: Lancer le test pour le voir échouer**

Run: `uv run pytest tests/test_jobs.py -v`
Expected: FAIL avec `ModuleNotFoundError: No module named 'pypl2mp3.web.jobs'`

- [ ] **Step 3: Écrire le registre**

Créer `src/pypl2mp3/web/jobs.py` :

```python
#!/usr/bin/env python3
"""In-process registry for long-running operations.

No broker and no database: this is a single-user local tool, and the
filesystem is the source of truth. A job that dies with the server loses
nothing that a fresh sync cannot recover — already written MP3 files stay
valid.

Each job keeps a bounded ring of its recent events so a browser that
reconnects mid-import can catch up instead of starting blind.
"""

import asyncio
from collections import deque
from dataclasses import dataclass, field
from enum import Enum
from typing import Awaitable, Callable, Optional

DEFAULT_MAX_EVENTS = 500


class JobState(str, Enum):
    PENDING = "pending"
    RUNNING = "running"
    COMPLETED = "completed"
    FAILED = "failed"
    CANCELLED = "cancelled"


class JobAlreadyRunning(Exception):
    """A job with this identifier is already active."""


@dataclass
class Job:
    """One long-running operation and everything observers need from it."""

    job_id: str
    state: JobState = JobState.PENDING
    result: object = None
    error: Optional[str] = None
    max_events: int = DEFAULT_MAX_EVENTS
    _events: deque = field(default_factory=deque, repr=False)
    task: Optional[asyncio.Task] = field(default=None, repr=False)

    def __post_init__(self) -> None:
        self._events = deque(maxlen=self.max_events)

    @property
    def events(self) -> list[dict]:
        return list(self._events)

    def append_event(self, event: dict) -> None:
        self._events.append(event)


class JobRegistry:
    """Track jobs by identifier for the lifetime of the server process."""

    def __init__(self, max_events: int = DEFAULT_MAX_EVENTS):
        self._jobs: dict[str, Job] = {}
        self._max_events = max_events

    def start(
        self,
        job_id: str,
        coro_factory: Callable[[Job], Awaitable[object]],
    ) -> Job:
        """Start a job, refusing to run one identifier twice at a time.

        Raises:
            JobAlreadyRunning: if that identifier is pending or running.
        """

        existing = self._jobs.get(job_id)
        if existing is not None and existing.state in {
            JobState.PENDING,
            JobState.RUNNING,
        }:
            raise JobAlreadyRunning(job_id)

        job = Job(job_id=job_id, max_events=self._max_events)
        self._jobs[job_id] = job
        job.task = asyncio.ensure_future(self._run(job, coro_factory))

        return job

    async def _run(
        self,
        job: Job,
        coro_factory: Callable[[Job], Awaitable[object]],
    ) -> None:
        job.state = JobState.RUNNING
        try:
            job.result = await coro_factory(job)
        except asyncio.CancelledError:
            # Partial work is preserved: whatever reached disk stays valid.
            job.state = JobState.CANCELLED
            raise
        except Exception as exc:
            job.state = JobState.FAILED
            job.error = f"{type(exc).__name__}: {exc}"
        else:
            job.state = JobState.COMPLETED

    def get(self, job_id: str) -> Optional[Job]:
        return self._jobs.get(job_id)

    def emit(self, job_id: str, event: dict) -> None:
        """Record an event. Silently ignores unknown jobs.

        Called from the progress port, possibly from a worker thread, so it
        must stay cheap and must never raise into the caller.
        """

        job = self._jobs.get(job_id)
        if job is not None:
            job.append_event(event)

    def cancel(self, job_id: str) -> bool:
        job = self._jobs.get(job_id)
        if job is None or job.task is None or job.task.done():
            return False

        return job.task.cancel()

    async def wait(self, job_id: str) -> None:
        """Await a job's completion. Test helper; never used by routes."""

        job = self._jobs.get(job_id)
        if job is None or job.task is None:
            return

        try:
            await job.task
        except asyncio.CancelledError:
            pass
```

- [ ] **Step 4: Lancer les tests**

Run: `uv run pytest tests/test_jobs.py -v`
Expected: 6 PASS

- [ ] **Step 5: Contre-épreuve du tampon borné**

Le test du tampon doit contraindre réellement. Remplacer temporairement
`deque(maxlen=self.max_events)` par `deque()` dans `__post_init__`.

Run: `uv run pytest tests/test_jobs.py -v`
Expected: `test_the_event_buffer_is_bounded` FAIL.

Puis restaurer : `git checkout src/pypl2mp3/web/jobs.py` — attention, ne pas exécuter cette commande avant d'avoir committé le reste, sinon tout le fichier est perdu. Faire cette contre-épreuve en modifiant puis en réécrivant la ligne à la main.

- [ ] **Step 6: Commit**

```bash
git add src/pypl2mp3/web/jobs.py tests/test_jobs.py
git commit -m "feat: add an in-process registry for long-running jobs"
```

---

### Task 5: WebProgress, sûr entre threads

`libs/song.py` dort de façon synchrone dans son chemin de progression (`update_progress_bar` anime tout saut de plus de dix points avec `time.sleep(0.01)` par point). Les téléchargements tourneront donc dans un thread de travail, et le `ProgressPort` sera appelé **depuis ce thread**. Toute remise d'événement à la boucle doit passer par `call_soon_threadsafe`.

**Files:**
- Create: `src/pypl2mp3/web/web_progress.py`
- Create: `tests/test_web_progress.py`

**Interfaces:**
- Consumes: `pypl2mp3.ports.progress.ProgressPort` (protocole, méthodes synchrones), `pypl2mp3.web.jobs.JobRegistry`.
- Produces: `WebProgress(registry: JobRegistry, job_id: str, loop: asyncio.AbstractEventLoop)` — implémente `ProgressPort`, dépose ses événements dans le job via la boucle.

- [ ] **Step 1: Écrire le test**

Créer `tests/test_web_progress.py` :

```python
"""Progress events must reach the job from a worker thread, safely."""

import asyncio

from pypl2mp3.ports.progress import ProgressPort
from pypl2mp3.web.jobs import Job, JobRegistry
from pypl2mp3.web.web_progress import WebProgress


async def test_it_satisfies_the_progress_port():
    registry = JobRegistry()
    progress = WebProgress(registry, "j1", asyncio.get_running_loop())

    assert isinstance(progress, ProgressPort)


async def test_events_reach_the_job():
    registry = JobRegistry()
    loop = asyncio.get_running_loop()

    async def work(job: Job) -> None:
        progress = WebProgress(registry, "j1", loop)
        progress.stage_started("download_audio", "Streaming audio:")
        progress.stage_progress("download_audio", 42.0)
        progress.stage_done("download_audio")
        await asyncio.sleep(0)  # let the loop drain the scheduled callbacks

    registry.start("j1", work)
    await registry.wait("j1")
    await asyncio.sleep(0)

    kinds = [event["kind"] for event in registry.get("j1").events]
    assert kinds == ["stage_started", "stage_progress", "stage_done"]


async def test_events_emitted_from_a_worker_thread_reach_the_job():
    """The real caller is song.py, running inside asyncio.to_thread."""

    registry = JobRegistry()
    loop = asyncio.get_running_loop()

    async def work(job: Job) -> None:
        progress = WebProgress(registry, "j1", loop)

        def in_thread() -> None:
            progress.stage_progress("download_audio", 10.0)

        await asyncio.to_thread(in_thread)
        await asyncio.sleep(0)

    registry.start("j1", work)
    await registry.wait("j1")
    await asyncio.sleep(0)

    events = registry.get("j1").events
    assert any(event["kind"] == "stage_progress" for event in events)


async def test_the_port_never_blocks_its_caller():
    """song.py calls this inside download loops; blocking would throttle it."""

    registry = JobRegistry()
    progress = WebProgress(registry, "j1", asyncio.get_running_loop())

    started = asyncio.get_running_loop().time()
    for index in range(1000):
        progress.stage_progress("download_audio", float(index % 100))
    elapsed = asyncio.get_running_loop().time() - started

    assert elapsed < 0.5, f"1000 emissions took {elapsed:.3f}s"


async def test_song_identified_carries_the_match():
    registry = JobRegistry()
    loop = asyncio.get_running_loop()

    async def work(job: Job) -> None:
        WebProgress(registry, "j1", loop).song_identified(
            "The Pharcyde", "Passin' Me By", 66.0
        )
        await asyncio.sleep(0)

    registry.start("j1", work)
    await registry.wait("j1")
    await asyncio.sleep(0)

    identified = [
        event
        for event in registry.get("j1").events
        if event["kind"] == "song_identified"
    ]
    assert identified[0]["artist"] == "The Pharcyde"
    assert identified[0]["score"] == 66.0
```

- [ ] **Step 2: Lancer le test pour le voir échouer**

Run: `uv run pytest tests/test_web_progress.py -v`
Expected: FAIL avec `ModuleNotFoundError: No module named 'pypl2mp3.web.web_progress'`

- [ ] **Step 3: Écrire l'implémentation**

Créer `src/pypl2mp3/web/web_progress.py` :

```python
#!/usr/bin/env python3
"""ProgressPort implementation that feeds a job's event buffer.

Two constraints shape this file.

First, the port's methods are synchronous and must never block: song.py
calls them from inside download loops, where waiting would throttle the
transfer.

Second, downloads run inside asyncio.to_thread, because song.py sleeps
synchronously in its own progress path (update_progress_bar animates any
jump over ten points with time.sleep(0.01) per point). So these methods are
called from a worker thread and must hand events back to the event loop
with call_soon_threadsafe rather than touching loop state directly.
"""

import asyncio

from pypl2mp3.web.jobs import JobRegistry


class WebProgress:
    """Turn progress callbacks into job events, safely across threads."""

    def __init__(
        self,
        registry: JobRegistry,
        job_id: str,
        loop: asyncio.AbstractEventLoop,
    ):
        self._registry = registry
        self._job_id = job_id
        self._loop = loop

    def _emit(self, event: dict) -> None:
        """Schedule the event on the loop. Returns immediately."""

        self._loop.call_soon_threadsafe(
            self._registry.emit, self._job_id, event
        )

    def stage_started(self, stage: str, label: str) -> None:
        self._emit({"kind": "stage_started", "stage": stage, "label": label})

    def stage_progress(self, stage: str, percent: float) -> None:
        self._emit(
            {"kind": "stage_progress", "stage": stage, "percent": percent}
        )

    def stage_done(self, stage: str) -> None:
        self._emit({"kind": "stage_done", "stage": stage})

    def song_identified(
        self, artist: str, title: str, score: float
    ) -> None:
        self._emit(
            {
                "kind": "song_identified",
                "artist": artist,
                "title": title,
                "score": score,
            }
        )
```

- [ ] **Step 4: Lancer les tests**

Run: `uv run pytest tests/test_web_progress.py -v`
Expected: 5 PASS

- [ ] **Step 5: Contre-épreuve de la sûreté entre threads**

Remplacer temporairement le corps de `_emit` par un appel direct :

```python
        self._registry.emit(self._job_id, event)
```

Run: `uv run pytest tests/test_web_progress.py -v`
Expected: les tests passent quand même — `emit` ne touche pas à la boucle, donc l'appel direct fonctionne **par accident** dans ce cas précis.

C'est une information à consigner, pas un échec : la sûreté entre threads n'est pas garantie par les tests, elle l'est par construction. Restaurer `call_soon_threadsafe` et **écrire ce constat dans le rapport** : si un jour `emit` touche à la boucle (pour réveiller un WebSocket, par exemple), l'appel direct deviendra un bug que la suite ne verra pas.

- [ ] **Step 6: Commit**

```bash
git add src/pypl2mp3/web/web_progress.py tests/test_web_progress.py
git commit -m "feat: feed job events from the progress port, thread-safely"
```

---

### Task 6: Le job « chercher les nouveautés », de bout en bout

Le consommateur réel qui exerce les tâches 4 et 5. Compare la playlist locale à la playlist YouTube et rapporte ce qui manque — long, réseau, avec progression.

**Files:**
- Create: `src/pypl2mp3/services/check_new_songs.py`
- Modify: `src/pypl2mp3/web/app.py`
- Modify: `src/pypl2mp3/web/templates/playlists.html`
- Create: `tests/test_check_new_songs.py`

**Interfaces:**
- Consumes: `ProgressPort`, `pypl2mp3.libs.repository.get_repository_playlist`, `pytubefix.Playlist`.
- Produces:
  - `NewSongsReport` — dataclass gelée : `playlist_id: str`, `total_remote: int`, `already_local: int`, `missing: list[str]` (identifiants vidéo)
  - `check_new_songs(repository_path: Path, playlist_id: str, progress: ProgressPort) -> NewSongsReport`
  - route `POST /playlists/{playlist_id}/check` démarrant le job
  - route `GET /jobs/{job_id}` rendant l'état

- [ ] **Step 1: Écrire le test**

Créer `tests/test_check_new_songs.py` :

```python
"""Comparing local against remote: the first genuinely long web job."""

from pathlib import Path

import pytest

from pypl2mp3.services import check_new_songs as mod
from pypl2mp3.services.check_new_songs import NewSongsReport, check_new_songs

from tests.doubles import FakeProgress

PLAYLIST_ID = "PLP6XxNg42qDGMg1cR2PPPzwdoAOD1MQ97"
REMOTE_IDS = ["AAAAAAAAAAA", "BBBBBBBBBBB", "CCCCCCCCCCC"]


class _FakePlaylist:
    def __init__(self, url, *args, **kwargs):
        self.title = "fake"
        self.owner = "owner"
        self.length = len(REMOTE_IDS)
        self.video_urls = [
            f"https://www.youtube.com/watch?v={vid}" for vid in REMOTE_IDS
        ]


def _make_local(repo: Path, present: list[str]) -> None:
    folder = repo / f"owner - fake [{PLAYLIST_ID}]"
    folder.mkdir(parents=True)
    for vid in present:
        (folder / f"ARTIST - Title [{vid}].mp3").touch()


def test_reports_only_the_missing_videos(tmp_path, monkeypatch):
    monkeypatch.setattr(mod, "Playlist", _FakePlaylist)
    _make_local(tmp_path, ["AAAAAAAAAAA"])

    report = check_new_songs(tmp_path, PLAYLIST_ID, FakeProgress())

    assert isinstance(report, NewSongsReport)
    assert report.total_remote == 3
    assert report.already_local == 1
    assert sorted(report.missing) == ["BBBBBBBBBBB", "CCCCCCCCCCC"]


def test_reports_nothing_missing_when_local_is_complete(tmp_path, monkeypatch):
    monkeypatch.setattr(mod, "Playlist", _FakePlaylist)
    _make_local(tmp_path, REMOTE_IDS)

    report = check_new_songs(tmp_path, PLAYLIST_ID, FakeProgress())

    assert report.missing == []


def test_it_reports_progress(tmp_path, monkeypatch):
    monkeypatch.setattr(mod, "Playlist", _FakePlaylist)
    _make_local(tmp_path, [])
    progress = FakeProgress()

    check_new_songs(tmp_path, PLAYLIST_ID, progress)

    kinds = [event[0] for event in progress.events]
    assert "stage_started" in kinds
    assert "stage_done" in kinds


def test_a_remote_failure_surfaces_rather_than_reporting_zero(
    tmp_path, monkeypatch
):
    """Reporting "nothing new" after a network failure would be a lie."""

    class _Failing:
        def __init__(self, url, *args, **kwargs):
            raise RuntimeError("network down")

    monkeypatch.setattr(mod, "Playlist", _Failing)
    _make_local(tmp_path, [])

    with pytest.raises(Exception):
        check_new_songs(tmp_path, PLAYLIST_ID, FakeProgress())
```

- [ ] **Step 2: Lancer le test pour le voir échouer**

Run: `uv run pytest tests/test_check_new_songs.py -v`
Expected: FAIL avec `ModuleNotFoundError: No module named 'pypl2mp3.services.check_new_songs'`

- [ ] **Step 3: Écrire le service**

Créer `src/pypl2mp3/services/check_new_songs.py` :

```python
#!/usr/bin/env python3
"""Compare a local playlist against its YouTube source.

This is the one operation whose duration is genuinely unpredictable: it
depends entirely on the link quality at the time of the call. That is why
it never runs on the synchronous path of a page render — it runs as a job,
and its result is cached and timestamped by the caller.
"""

from dataclasses import dataclass
from pathlib import Path

from pytubefix import Playlist

from pypl2mp3.libs.repository import get_repository_playlist
from pypl2mp3.libs.utils import get_song_id_from_filename, get_song_id_from_url
from pypl2mp3.ports.progress import ProgressPort

STAGE = "check_new_songs"


@dataclass(frozen=True)
class NewSongsReport:
    """What the remote playlist has that the local folder does not."""

    playlist_id: str
    total_remote: int
    already_local: int
    missing: list[str]


def check_new_songs(
    repository_path: Path,
    playlist_id: str,
    progress: ProgressPort,
) -> NewSongsReport:
    """List the videos present remotely but missing locally.

    Raises:
        Exception: whatever pytubefix raises if the playlist cannot be
            reached. Never swallowed — reporting "nothing new" after a
            network failure would be a lie.
    """

    progress.stage_started(STAGE, "Checking for new songs:")

    selected = get_repository_playlist(
        Path(repository_path), playlist_id, must_exist=False
    )

    # pytubefix fetches lazily; every attribute read below can raise.
    playlist = Playlist(selected.url)
    remote_ids = [
        song_id
        for song_id in map(get_song_id_from_url, playlist.video_urls)
        if song_id is not None
    ]

    playlist_folder = Path(repository_path) / (
        f"{playlist.owner} - {playlist.title} [{selected.id}]"
    )
    local_ids = {
        song_id
        for song_id in map(
            get_song_id_from_filename,
            playlist_folder.glob("*.mp3"),
        )
        if song_id is not None
    }

    missing = [song_id for song_id in remote_ids if song_id not in local_ids]

    progress.stage_progress(STAGE, 100.0)
    progress.stage_done(STAGE)

    return NewSongsReport(
        playlist_id=selected.id,
        total_remote=len(remote_ids),
        already_local=len(remote_ids) - len(missing),
        missing=missing,
    )
```

- [ ] **Step 4: Lancer les tests du service**

Run: `uv run pytest tests/test_check_new_songs.py -v`
Expected: 4 PASS

Si `get_song_id_from_filename` renvoie `None` pour les chemins produits par le
test, lire sa signature dans `src/pypl2mp3/libs/utils.py` — elle attend
peut-être un nom et non un `Path`. Adapter l'appel du service, pas le test.

- [ ] **Step 5: Brancher la route et le job**

Dans `src/pypl2mp3/web/app.py`, ajouter aux imports :

```python
import asyncio

from fastapi import HTTPException

from pypl2mp3.services.check_new_songs import check_new_songs
from pypl2mp3.web.jobs import JobAlreadyRunning, JobRegistry
from pypl2mp3.web.web_progress import WebProgress
```

Dans `create_app`, après la création de `templates` :

```python
    app.state.jobs = JobRegistry()

    @app.post("/playlists/{playlist_id}/check")
    async def start_check(playlist_id: str) -> dict[str, str]:
        loop = asyncio.get_running_loop()
        repository_path = app.state.repository_path

        async def work(job) -> dict:
            progress = WebProgress(app.state.jobs, job.job_id, loop)
            # Run in a worker thread: song.py and pytubefix block
            # synchronously, and the event loop must stay responsive.
            report = await asyncio.to_thread(
                check_new_songs, repository_path, playlist_id, progress
            )
            return {
                "total_remote": report.total_remote,
                "already_local": report.already_local,
                "missing": report.missing,
            }

        try:
            job = app.state.jobs.start(f"check:{playlist_id}", work)
        except JobAlreadyRunning:
            raise HTTPException(
                status_code=409, detail="already checking this playlist"
            )

        return {"job_id": job.job_id}

    @app.get("/jobs/{job_id}")
    def job_status(job_id: str) -> dict:
        job = app.state.jobs.get(job_id)
        if job is None:
            raise HTTPException(status_code=404, detail="unknown job")

        return {
            "state": job.state.value,
            "events": job.events,
            "result": job.result,
            "error": job.error,
        }
```

- [ ] **Step 6: Ajouter le bouton dans le gabarit**

Dans `src/pypl2mp3/web/templates/playlists.html`, à l'intérieur de la première
`<td>` de la boucle, sous le `<span class="id">` :

```html
              <button
                hx-post="/playlists/{{ summary.playlist_id }}/check"
                hx-swap="none"
              >Check for new songs</button>
```

Le résultat n'est pas encore affiché dans la page : cette étape prouve que le
job démarre. Le rendu vivant du résultat relève du plan 3, avec le WebSocket.
**Consigner cette limite dans le rapport** plutôt que de la laisser deviner.

- [ ] **Step 7: Écrire le test des routes**

Ajouter à la fin de `tests/test_check_new_songs.py` :

```python
async def test_the_route_starts_a_job_and_exposes_its_status(
    tmp_path, monkeypatch
):
    import httpx

    from pypl2mp3.web.app import create_app

    monkeypatch.setattr(mod, "Playlist", _FakePlaylist)
    _make_local(tmp_path, [])
    app = create_app(tmp_path)

    # An ASYNC client, deliberately: the route schedules a background task on
    # the running loop. A sync httpx.Client drives the app through a
    # short-lived portal loop, so the task would be orphaned between the two
    # requests and the status read would be meaningless.
    transport = httpx.ASGITransport(app=app)
    async with httpx.AsyncClient(
        transport=transport, base_url="http://test"
    ) as client:
        started = await client.post(f"/playlists/{PLAYLIST_ID}/check")
        assert started.status_code == 200
        job_id = started.json()["job_id"]
        assert job_id == f"check:{PLAYLIST_ID}"

        status = await client.get(f"/jobs/{job_id}")
        assert status.status_code == 200
        assert status.json()["state"] in {"pending", "running", "completed"}

        assert (await client.get("/jobs/nope")).status_code == 404


async def test_a_second_check_on_the_same_playlist_is_refused(
    tmp_path, monkeypatch
):
    """Two checks on one playlist would duplicate the network work."""

    import asyncio

    import httpx

    from pypl2mp3.web.app import create_app

    # A playlist that never finishes, so the first job is still running when
    # the second request arrives. Without this the race is unwinnable.
    class _SlowPlaylist(_FakePlaylist):
        def __init__(self, url, *args, **kwargs):
            super().__init__(url, *args, **kwargs)
            import time

            time.sleep(2)

    monkeypatch.setattr(mod, "Playlist", _SlowPlaylist)
    _make_local(tmp_path, [])
    app = create_app(tmp_path)

    transport = httpx.ASGITransport(app=app)
    async with httpx.AsyncClient(
        transport=transport, base_url="http://test"
    ) as client:
        assert (
            await client.post(f"/playlists/{PLAYLIST_ID}/check")
        ).status_code == 200

        # Let the job reach RUNNING before asking again.
        await asyncio.sleep(0.1)

        second = await client.post(f"/playlists/{PLAYLIST_ID}/check")
        assert second.status_code == 409

        app.state.jobs.cancel(f"check:{PLAYLIST_ID}")
```

- [ ] **Step 8: Lancer toute la suite**

Run: `uv run pytest -v`
Expected: tous verts. Le décompte exact dépend des tâches précédentes ; le noter.

- [ ] **Step 9: Vérifier de bout en bout dans un navigateur**

```bash
uv run pypl2mp3 ui -r ~/Desktop/playlists
```

Ouvrir `http://127.0.0.1:8731/`, cliquer « Check for new songs » sur la petite
playlist mid90s, puis interroger le job :

```bash
curl -s http://127.0.0.1:8731/jobs/check:PLP6XxNg42qDGMg1cR2PPPzwdoAOD1MQ97
```

Expected: un état qui progresse jusqu'à `completed`, avec un `result` listant
les identifiants manquants. **Confirmer que la page reste réactive pendant que
le job tourne** — c'est tout l'intérêt du thread de travail. Recharger la page
pendant l'exécution : elle doit répondre immédiatement.

- [ ] **Step 10: Commit**

```bash
git add src/pypl2mp3/services/check_new_songs.py \
        src/pypl2mp3/web/app.py \
        src/pypl2mp3/web/templates/playlists.html \
        tests/test_check_new_songs.py
git commit -m "feat: check a playlist for new songs as a background job"
```

---

## Ce que ce plan ne fait pas

- **Pas de WebSocket.** Le registre bufferise déjà les événements et
  `GET /jobs/{id}` les rend. Le canal poussé, et avec lui `WebInteraction`,
  arrivent au plan 3 — quand `import` apportera de vraies questions à poser.
  Construire `WebInteraction` maintenant reviendrait à livrer un composant sans
  consommateur, exactement l'erreur qui a produit trois défauts dans
  `song_callbacks` au plan 1.
- **Pas de migration de commande.** Les sept commandes restantes gardent leur
  forme actuelle.
- **Aucune modification de `libs/`.** Le blocage synchrone de
  `update_progress_bar` est contourné par `asyncio.to_thread`, pas corrigé.
- **Pas de mise en cache du résultat.** Le spec prévoit un résultat horodaté et
  mis en cache ; ici le job est refait à chaque demande. Suffisant pour prouver
  la chaîne, à compléter au plan 3.

---

### Task 7: Retour visuel minimal du job

Ajoutée après la tâche 6, sur constat de revue : le bouton démarre bien le job,
mais `hx-swap="none"` jette la réponse et rien n'indique quoi que ce soit à
l'écran. Cliquer paraît sans effet — y compris quand le job échoue.

Le canal poussé reste au plan 3. Ici, le fragment s'auto-interroge.

**Files:**
- Create: `src/pypl2mp3/web/templates/job.html`
- Modify: `src/pypl2mp3/web/app.py`
- Modify: `src/pypl2mp3/web/templates/playlists.html`
- Modify: `src/pypl2mp3/web/templates/base.html`
- Create: `tests/test_web_job_feedback.py`

**Interfaces:**
- Consumes: `JobRegistry`, `JobState`, la route `POST /playlists/{id}/check` et `GET /jobs/{job_id}` de la tâche 6.
- Produces: les deux mêmes routes, qui **négocient** désormais leur réponse sur l'en-tête `HX-Request` — fragment HTML pour HTMX, JSON inchangé sinon.

**Principe.** Les tests de la tâche 6 interrogent ces routes sans en-tête HTMX :
ils doivent continuer de passer **sans modification**. L'API JSON reste le
contrat ; l'interface se greffe à côté.

- [ ] **Step 1: Écrire le test**

Créer `tests/test_web_job_feedback.py` :

```python
"""Clicking the button must show something — including when it fails."""

import asyncio

import httpx
import pytest

from pypl2mp3.services import check_new_songs as mod
from pypl2mp3.web.app import create_app

PLAYLIST_ID = "PLP6XxNg42qDGMg1cR2PPPzwdoAOD1MQ97"
REMOTE_IDS = ["AAAAAAAAAAA", "BBBBBBBBBBB"]
HX = {"HX-Request": "true"}


class _FakePlaylist:
    def __init__(self, url, *args, **kwargs):
        self.title = "fake"
        self.owner = "owner"
        self.length = len(REMOTE_IDS)
        self.video_urls = [
            f"https://www.youtube.com/watch?v={vid}" for vid in REMOTE_IDS
        ]


class _FailingPlaylist:
    def __init__(self, url, *args, **kwargs):
        raise RuntimeError("network down")


def _make_local(repo, present):
    folder = repo / f"owner - fake [{PLAYLIST_ID}]"
    folder.mkdir(parents=True)
    for vid in present:
        (folder / f"ARTIST - Title [{vid}].mp3").touch()


def _client(app):
    return httpx.AsyncClient(
        transport=httpx.ASGITransport(app=app), base_url="http://test"
    )


async def _settle(client, job_id):
    """Poll until the job leaves its running states, or fail loudly."""

    for _ in range(50):
        body = (await client.get(f"/jobs/{job_id}", headers=HX)).text
        if "hx-get" not in body:
            return body
        await asyncio.sleep(0.05)

    pytest.fail(f"job {job_id} never settled")


async def test_the_json_api_is_unchanged_without_the_htmx_header(
    tmp_path, monkeypatch
):
    """The task 6 contract must survive: no header, still JSON."""

    monkeypatch.setattr(mod, "Playlist", _FakePlaylist)
    _make_local(tmp_path, [])

    async with _client(create_app(tmp_path)) as client:
        response = await client.post(f"/playlists/{PLAYLIST_ID}/check")

    assert response.headers["content-type"].startswith("application/json")
    assert response.json()["job_id"] == f"check:{PLAYLIST_ID}"


async def test_the_button_gets_a_fragment_that_polls_itself(
    tmp_path, monkeypatch
):
    monkeypatch.setattr(mod, "Playlist", _FakePlaylist)
    _make_local(tmp_path, [])

    async with _client(create_app(tmp_path)) as client:
        body = (
            await client.post(f"/playlists/{PLAYLIST_ID}/check", headers=HX)
        ).text

    assert "<div" in body
    assert f'hx-get="/jobs/check:{PLAYLIST_ID}"' in body
    assert "hx-trigger" in body


async def test_polling_stops_once_the_job_is_done(tmp_path, monkeypatch):
    """Without this, the browser would poll a finished job forever."""

    monkeypatch.setattr(mod, "Playlist", _FakePlaylist)
    _make_local(tmp_path, [])

    async with _client(create_app(tmp_path)) as client:
        await client.post(f"/playlists/{PLAYLIST_ID}/check", headers=HX)
        settled = await _settle(client, f"check:{PLAYLIST_ID}")

    assert "hx-trigger" not in settled
    assert "hx-get" not in settled


async def test_a_completed_check_reports_what_it_found(tmp_path, monkeypatch):
    monkeypatch.setattr(mod, "Playlist", _FakePlaylist)
    _make_local(tmp_path, ["AAAAAAAAAAA"])

    async with _client(create_app(tmp_path)) as client:
        await client.post(f"/playlists/{PLAYLIST_ID}/check", headers=HX)
        settled = await _settle(client, f"check:{PLAYLIST_ID}")

    assert "1" in settled


async def test_a_failed_check_shows_the_error_rather_than_nothing(
    tmp_path, monkeypatch
):
    """Silence after a failure is the worst outcome: it reads as success."""

    monkeypatch.setattr(mod, "Playlist", _FailingPlaylist)
    _make_local(tmp_path, [])

    async with _client(create_app(tmp_path)) as client:
        await client.post(f"/playlists/{PLAYLIST_ID}/check", headers=HX)
        settled = await _settle(client, f"check:{PLAYLIST_ID}")

    assert "network down" in settled or "failed" in settled.lower()


async def test_a_second_click_says_so_instead_of_looking_inert(
    tmp_path, monkeypatch
):
    """HTMX does not swap on a 4xx, so a bare 409 would show nothing."""

    class _Slow(_FakePlaylist):
        def __init__(self, url, *args, **kwargs):
            import time

            time.sleep(2)
            super().__init__(url, *args, **kwargs)

    monkeypatch.setattr(mod, "Playlist", _Slow)
    _make_local(tmp_path, [])
    app = create_app(tmp_path)

    async with _client(app) as client:
        await client.post(f"/playlists/{PLAYLIST_ID}/check", headers=HX)
        await asyncio.sleep(0.1)
        second = await client.post(
            f"/playlists/{PLAYLIST_ID}/check", headers=HX
        )

        assert second.status_code == 200
        assert "already" in second.text.lower()

        app.state.jobs.cancel(f"check:{PLAYLIST_ID}")
```

- [ ] **Step 2: Lancer le test pour le voir échouer**

Run: `uv run pytest tests/test_web_job_feedback.py -v`
Expected: échecs — les routes renvoient encore du JSON quelle que soit l'en-tête.

- [ ] **Step 3: Écrire le fragment**

Créer `src/pypl2mp3/web/templates/job.html` :

```html
<div id="job-{{ playlist_id }}" class="job"
     {% if polling %}hx-get="/jobs/{{ job_id }}"
     hx-trigger="every 1s"
     hx-swap="outerHTML"{% endif %}>
  {% if state in ("pending", "running") %}
    <span class="job-running">Checking…</span>
  {% elif state == "completed" %}
    {% if result and result.missing %}
      <span class="job-new">{{ result.missing | length }} new song(s)</span>
    {% else %}
      <span class="job-none">Up to date</span>
    {% endif %}
  {% elif state == "failed" %}
    <span class="job-error">Check failed: {{ error }}</span>
  {% elif state == "cancelled" %}
    <span class="job-error">Check cancelled</span>
  {% elif state == "busy" %}
    <span class="job-running">Already checking…</span>
  {% endif %}
</div>
```

`polling` est faux dès que l'état est terminal : le fragment renvoyé ne porte
plus `hx-trigger`, donc le navigateur cesse de lui-même. Pas de compteur, pas de
condition d'arrêt côté client.

- [ ] **Step 4: Négocier la réponse dans les deux routes**

Dans `src/pypl2mp3/web/app.py`, ajouter un helper au-dessus des routes, dans
`create_app` :

```python
    def _job_fragment(request, job_id, playlist_id, state, result, error):
        return templates.TemplateResponse(
            request,
            "job.html",
            {
                "job_id": job_id,
                "playlist_id": playlist_id,
                "state": state,
                "result": result,
                "error": error,
                "polling": state in ("pending", "running"),
            },
        )
```

Puis faire prendre `request: Request` aux deux routes existantes et, avant leur
`return` actuel, renvoyer le fragment quand `request.headers.get("HX-Request")`
est présent. Le chemin JSON reste **exactement** ce qu'il est aujourd'hui.

Pour le doublon refusé : au lieu de lever `HTTPException(409)` quand l'appel
vient d'HTMX, renvoyer le fragment avec `state="busy"` et un code 200 — HTMX
n'échange rien sur un 4xx, donc un 409 nu n'afficherait rien. Le chemin non-HTMX
continue de lever le 409.

- [ ] **Step 5: Brancher le bouton**

Dans `src/pypl2mp3/web/templates/playlists.html`, remplacer le bouton par :

```html
              <button
                hx-post="/playlists/{{ summary.playlist_id }}/check"
                hx-target="#job-{{ summary.playlist_id }}"
                hx-swap="outerHTML"
                hx-indicator="#job-{{ summary.playlist_id }}"
              >Check for new songs</button>
              <div id="job-{{ summary.playlist_id }}" class="job"></div>
```

- [ ] **Step 6: Styler les quatre états**

Ajouter au `<style>` de `base.html` :

```css
      .job { font-size: 0.85em; min-height: 1.2em; }
      .job-running { opacity: 0.7; }
      .job-new { color: #0a7d3f; font-weight: 600; }
      .job-none { opacity: 0.6; }
      .job-error { color: #b00020; }
      .htmx-request .job, .htmx-request.job { opacity: 0.5; }
```

- [ ] **Step 7: Lancer les tests**

Run: `uv run pytest tests/test_web_job_feedback.py -v`
Expected: 6 PASS. Puis la suite complète : les tests de la tâche 6 doivent
passer inchangés, puisqu'ils n'envoient pas l'en-tête HTMX.

- [ ] **Step 8: Contre-épreuves**

Trois mutations, chacune doit faire échouer un test. Restaurer à la main entre
chaque.

1. Retirer le `{% if polling %}` de `job.html`, laissant `hx-trigger` toujours
   présent → `test_polling_stops_once_the_job_is_done` doit ÉCHOUER.
2. Remplacer la branche `failed` par une chaîne vide →
   `test_a_failed_check_shows_the_error_rather_than_nothing` doit ÉCHOUER.
3. Rétablir `HTTPException(409)` pour le chemin HTMX →
   `test_a_second_click_says_so_instead_of_looking_inert` doit ÉCHOUER.

- [ ] **Step 9: Vérifier dans un vrai navigateur si possible**

Si l'environnement le permet, ouvrir la page et cliquer. Sinon, le consigner
honnêtement plutôt que de le supposer — les tâches précédentes n'ont jamais pu
rendre la page dans un moteur réel.

- [ ] **Step 10: Commit**

```bash
git add src/pypl2mp3/web/ tests/test_web_job_feedback.py
git commit -m "feat: show what the check job is doing"
```
